# 全网百度网盘资源搜索器 · 规划方案

> 目标：输入一个关键词，**并发打穿全网 N 个数据源**，把散落各处的百度网盘分享链接（含提取码）全部捞回来，去重、验活、排序，用一份干净的结果呈现。
> 核心指标不是"能搜"，而是 **召回率（找得全）+ 有效率（链接还活着）**。

规划日期：2026-09-12 ｜ 状态：待你确认几个关键选择后开工

---

## 0. 先说结论（本方案的三个关键判断）

1. **不要自己写几十个网盘站爬虫**。已有成熟聚合层 [PanSou](https://github.com/fish2018/pansou)（48 个网盘搜索插件 + 上千个 TG 频道，单次搜索可返回 1700+ 条链接）。我们把它当"数据源 A"，省下 80% 的维护成本。
2. **真正的增量在"聚合站搜不到的地方"**：B 站视频简介/评论区、贴吧、论坛帖子、豆瓣、以及搜索引擎里裸露的 `pan.baidu.com/s/xxx`。这是数据源 B / C，也是与市面网盘搜索器拉开差距的地方。
3. **百度链接可以免登录验活**（已实测），这让结果质量发生质变——能直接标出"这条失效了"，而不是把 2016 年的死链一股脑丢给你。

---

## 1. 网络环境实测（本机 macOS，2026-09-12）

上次那台机器连不上 `pan.baidu.com`，**本机没有这个问题**，方案可以直接落地。

### ✅ 可达（HTTP 200）

| 目标 | 用途 |
|---|---|
| `pan.baidu.com` | **核心**，链接可打开、可验活 |
| `api.bilibili.com` / `www.bilibili.com` | 视频简介 + 评论区抓链接 |
| `github.com` / `www.google.com` | 拉工具、搜索 |
| `www.pansoso.com`、`www.dalipan.com`、`www.dashengpan.com` | 网盘搜索站 |
| `www.tsdm39.com` | 天使动漫（搜索需登录） |
| `cn.bing.com`、`www.so.com`、`search.brave.com`、`html.duckduckgo.com` | 通用搜索引擎 |
| `so.252035.xyz` | PanSou 公共实例（**实测 API 可用**） |

### ❌ 不可达 / 受限

| 目标 | 状态 | 处理 |
|---|---|---|
| `www.yunpanjingling.com` | 000（DNS/TLS 失败） | 弃用 |
| `www.xiaobaipan.com` | 403（需 UA/Cookie） | 需带 UA + Cookie 再试 |
| `tieba.baidu.com` | 403（百度安全验证） | 贴吧改走移动端 API 或复用 PanSou |
| `raw.githubusercontent.com` | DNS 非公网 IP | 用镜像或 `github.com` |

### 🔑 实测：PanSou 公共 API 返回结构化结果

```
GET https://so.252035.xyz/api/search?kw=<关键词>
→ data.merged_by_type.{baidu|quark|aliyun|115|xunlei|123|tianyi|uc|pikpak|magnet}[]
   每项含：url（含 ?pwd= 提取码）、password、note、datetime、source（plugin:xxx 或 tg:频道名）
```

实测样例：

| 关键词 | 结果 | 覆盖的网盘类型 |
|---|---|---|
| 三体 | 55 条 | baidu 1、quark 10、aliyun 6、uc 4、123 3、pikpak 3、xunlei 2、magnet 26 |
| 周杰伦 | 117 条 | quark 69、**baidu 19**、115 11、aliyun 10、xunlei 3、tianyi 2、123 1、magnet 2 |

> ⚠️ 公共实例不稳定：实测有 `HTTP 400`、`read timeout`。→ **生产必须自建**（Docker 一键），公共实例只用于 MVP 验证。

### 🔑 实测：百度分享链接免登录验活（最关键发现）

```
GET https://pan.baidu.com/api/shorturlinfo?shorturl=<surl>&web=1&app_id=250528&channel=chunlei&clienttype=0&t=<ms>
→ {"shareid":..., "uk":..., "errno":<状态码>}
```

#### ⚠️ 关键坑（曾导致误判接口"已失效"）

百度 API 的 `surl` 参数是**去掉开头 `1`** 的部分。用完整 token 调 `share/verify` 会**恒返回 `105`**；换成去掉开头 `1` 的 surl 后立刻正常。

> 实测：完整 token 调 `shorturlinfo` 得到 `-9`/`-21` 这类**误导性**信号；换 stripped surl 后样本全部返回 `errno=2`（需提取码），再用 `share/verify` 配正确提取码验证 → **4/4 返回 `errno=0`**，证明这些链接全部真实有效。

#### 接口 1：`POST /share/verify` — 有提取码时的**权威校验**

```
POST https://pan.baidu.com/share/verify?surl=<surl去掉开头1>&t=<ms>&channel=chunlei&web=1&app_id=250528&clienttype=0
Referer: https://pan.baidu.com/share/init?surl=<同上>
Body:    pwd=<提取码>&vcode=&vcode_str=
```

| errno | 含义 | 判定 |
|---|---|---|
| `0`（并返回 `randsk`） | 提取码正确 | ✅ **完全可用，可直接一键打开** |
| `-12` | 提取码错误 | ⚠️ 链接可能存活，码不对 |
| `105` | 分享不存在 | ❌ 已失效 |

#### 接口 2：`GET /api/shorturlinfo` — 无提取码时判断存活

| errno | 判定 |
|---|---|
| `2` | ✅ 存活，需要提取码 |
| `0` | ✅ 存活，无需提取码 |
| `140` | ❌ 链接不存在 |
| `-3` | ❌ 已失效 |

**意义**：无需登录、无验证码，即可给出「**码正确的可用链接 / 需码存活 / 码错 / 已失效**」四态判定。这是本工具相对市面网盘搜索器的核心质量优势。

> 码表写成 `config/baidu_errno.yaml`，接口变动时改配置不改代码。

---

## 2. 总体架构：四环数据源 + 一条流水线

```
                    ┌──────────────── 输入：关键词 ────────────────┐
                    ▼                                              
  ┌── A 环：网盘聚合引擎 ──────┐   PanSou（自建）：48 插件 + TG 频道 → 覆盖最广
  ├── B 环：搜索引擎定向检索 ──┤   Google/Bing/360/DDG/Brave + site:pan.baidu.com
  ├── C 环：UGC 平台内搜 ─────┤   B站(简介+评论)、贴吧、豆瓣、论坛、TG
  └── D 环：本地私有索引 ─────┘   SQLite 沉淀历史结果，越用越全、越用越快
                    │
                    ▼
   抽取 → 归一化 → 去重 → 验活 → 排序 → 输出(CLI / Web / JSON / CSV)
```

### A 环：网盘聚合引擎（主力，负责"全"）

- **PanSou 自建**（Go + Docker，前端后端一体，开箱即用）
- 一次关键词并发打 48 个网盘搜索站 + 上千 TG 频道
- 返回值已经是分好类的结构化数据，`source` 字段还能溯源到具体插件/频道
- 我们的适配器只需调一个 HTTP 接口 → **投产比最高**

### B 环：搜索引擎定向检索（负责"野链接"）

论坛、博客、资源站的帖子正文里经常**直接裸露**网盘链接，聚合站抓不到。

查询模板（同一关键词 × 多模板 × 多引擎）：

```
site:pan.baidu.com/s <kw>
"<kw>" 百度网盘 提取码
"<kw>" pan.baidu.com
"<kw>" 百度云 分享 密码
"<kw>" 网盘 下载 -广告
```

- 引擎：Google / Bing / 360 / DuckDuckGo / Brave / Mojeek（实测均可达）
- 从结果页 HTML 用正则抽 `pan\.baidu\.com/s/[A-Za-z0-9_-]+`，并在**前后 200 字符窗口**内找提取码
- 代码可复用 `baidu-project/round2/engines.py` 原型（已验证多引擎抓取可用）

### C 环：UGC 平台内搜（链接的真实产地）

| 源 | 方式 | 备注 |
|---|---|---|
| **B 站** | `api.bilibili.com` 搜索 → 取视频简介 + 置顶评论 + 全量评论 | **最高价值**。已有原型 `round2/bili_*.py`（含 WBI 签名 `bili_wbi.py`、评论抓取 `comments_full.py`）可直接迁移 |
| 贴吧 | 移动端接口绕开 403 安全验证 | 待验证 |
| 豆瓣 / 知乎 / 微博 | HTML 适配器 | |
| 天使动漫 / hifini / 2cycd / nmbxd | HTML 适配器，部分需登录 | 需你提供账号或接受"手动登录态导入" |
| Telegram | PanSou 已覆盖大量频道；可补 TG 搜索 | |

### D 环：本地私有索引（可选，M4）

把每次搜索结果沉淀进 SQLite：

- **去重加速**：命中过的分享不再重复验活
- **增量刷新**：定时重验历史链接，失效的自动降权/标记
- **反向检索**：`pansearch index --list / --stats`，形成你自己的资源库

---

## 3. 处理流水线（Pipeline 细节）

### 3.1 抽取 extract
从 HTML / JSON 中抽出：分享链接、提取码、标题、文件大小、分享时间、来源 URL、来源类型。

### 3.2 归一化 normalize
统一百度网盘链接的四种形态：

| 原始形态 | 归一为 |
|---|---|
| `https://pan.baidu.com/s/1AbCdEf?pwd=1234` | surl=`1AbCdEf`，pwd=`1234` |
| `https://pan.baidu.com/share/init?surl=1AbCdEf` | surl=`1AbCdEf` |
| `https://pan.baidu.com/s/1AbCdEf#1234` | surl=`1AbCdEf`，pwd=`1234` |
| `https://pan.baidu.com/share/link?shareid=xxx&uk=yyy` | 转 surl |
| `pan.baidu.com%2Fs%2F1AbCdEf`（URL 编码） | 解码后归一 |

提取码写法也要归一：`pwd=1234` / `提取码: 1234` / `提取码：1234` / `密码 1234` / `访问码：1234` / `（1234）`。

### 3.3 去重 dedupe
两层指纹：

1. **分享指纹** = `surl`（同一分享被 A/B/C 三环都搜到时合并，`sources` 列表保留多个来源 → 来源越多越可信，参与排序加分）
2. **资源指纹** = 规范化标题 + 文件大小（识别"同一份资源的不同分享"，标为`同资源候选`而不是直接删）

### 3.4 验活 verify ← **质量分水岭**
- 并发调 `shorturlinfo`，对结果做 errno → 状态映射（YAML 可配置）
- 状态：`alive` / `need_password` / `dead` / `not_found` / `unknown`
- **TTL 缓存 6 小时**（同一链接不重复打）；限速（如 5 QPS）+ 失败重试
- 结果随时间变化 → 输出时标注"校验时间"

### 3.5 排序 score
```
score = 相关性(关键词命中/出现在标题前部)
      × 源权重(PanSou 插件 1.0 / TG 0.9 / B站 0.8 / 论坛 0.8 / 搜索引擎 0.6)
      × 存活(存活且提取码齐全 > 存活需码 > 未知 > 失效=直接沉底)
      × 新鲜度(分享时间衰减)
      + 多源命中加成(≥2 个独立来源命中同一 surl)
```

### 3.6 输出 render
- **CLI**：表格 + 颜色（绿=活 / 黄=需码 / 灰=死）
- **Web UI**：搜索框、按关键词/网盘类型筛选、一键复制"链接+提取码"、失效灰显折叠、来源可点回原帖
- **API**：`GET /api/search?kw=&types=baidu&alive_only=1` 返回 JSON
- **导出**：CSV / Markdown（沿用你 `ADHouse_专辑清单与链接收集表.csv` 的字段风格：专辑名/链接/提取码/来源页面/有效or失效）

---

## 4. 技术栈

**推荐 Python**——理由：`baidu-project/round2/` 里已有 789 行可复用原型（多引擎抓取、B 站 WBI 签名、评论全量抓取、小白盘爬取、结果解析），迁到 Python 是**零重写成本**；Node 则要重写。

| 层 | 选型 |
|---|---|
| 运行时 | Python 3.12+，`uv` 管依赖 |
| HTTP | `httpx`（异步并发、HTTP/2） |
| 解析 | `selectolax`（快）或 `parsel`（CSS/XPath 顺手） |
| 数据模型 | `pydantic` v2 |
| 存储 | SQLite + `SQLModel` |
| CLI | `typer` + `rich` |
| Web | `FastAPI` + Jinja2 + 原生 JS（**零构建**，`pansearch web` 一条命令起服务） |
| 浏览器自动化 | `playwright`（**仅**用于极少数 JS 渲染站点，默认关闭） |
| 可选依赖 | `docker compose`（跑自建 PanSou） |

> 为什么默认不用 Playwright：几十个站点都上无头浏览器，速度慢、易被封、调试成本高。**HTTP + 接口优先，渲染兜底**。

---

## 5. 目录结构

```
pan-sousuo/
├── pyproject.toml
├── README.md
├── config/
│   ├── sources.yaml          # 数据源开关 / 权重 / 限速 / 超时
│   └── baidu_errno.yaml      # 百度 errno 码表（M1 实测校准）
├── src/pansearch/
│   ├── models.py             # SearchHit / Resource / VerifyResult
│   ├── adapters/
│   │   ├── base.py           # Adapter 基类 + 注册表（新增源只需加一个文件）
│   │   ├── pansou.py         # A 环
│   │   ├── websearch.py      # B 环（6 引擎）
│   │   ├── bilibili.py       # C 环（简介 + 评论 + WBI）
│   │   ├── tieba.py
│   │   ├── douban.py
│   │   └── forums.py
│   ├── extract.py            # 链接 / 提取码 / 大小 / 时间 抽取
│   ├── normalize.py          # URL & 提取码归一
│   ├── dedupe.py
│   ├── verify.py             # 百度链接验活 + TTL 缓存 + 限速
│   ├── score.py
│   ├── store.py              # SQLite（D 环）
│   ├── pipeline.py           # 并发编排 + 单源失败隔离
│   ├── cli.py                # pansearch search "关键词"
│   └── web/                  # FastAPI + 模板
└── tests/
    ├── fixtures/             # 真实链接样本（含已知失效/需码）
    └── test_*.py
```

**插件化设计**：新数据源 = 新增一个 `adapters/xxx.py` 实现 `search(kw) -> list[SearchHit]`，在 `sources.yaml` 开一行即可。单个源超时/报错**不影响整体结果**，并记录健康度（成功率 / 平均耗时）。

---

## 6. 分期交付

| 阶段 | 内容 | 状态 |
|---|---|---|
| **M0** | 项目骨架 + 数据模型 + 配置 | ✅ 完成 |
| **M1 MVP** | PanSou 适配器 + 抽取/归一/去重 + **百度验活** + CLI | ✅ 完成（实测 22 条百度链接全部验活，19s） |
| **M2 覆盖翻倍** | 搜索引擎两阶段适配器（Brave/360 → 内容页）+ 补 B站 | 🟡 搜索引擎部分完成；**B站未接入**（API 返回 412，需 WBI 签名） |
| **M3 好用** | Web UI + CSV/JSON 导出 + 结果筛选/复制 | ✅ 完成（`pansearch web` → http://127.0.0.1:8765） |
| **M4 越用越全** | SQLite 索引沉淀 + 定时重验 + 论坛适配器 + 自建 PanSou Docker | 🟡 验活 TTL 缓存已做；其余待做 |
| **M5 可选** | 批量校验 / 浏览器扩展 / 转存（**不做**） | — |

### 下一步优先级（按投产比）

1. **自建 PanSou Docker** —— 公共实例偶发 400/timeout；自建后可拿满 48 插件 + 不限流，是提升召回最有效的一步
2. **自建 SearXNG** —— 替代 Brave，支持 JSON API 且能聚合多引擎，解决 429 与单点依赖
3. **B 站适配器（WBI 签名）** —— `baidu-project/round2/` 已有原型可迁移，视频简介与评论区是网盘链接高产来源
4. **社区资源再分发** —— 把 `pansearch verify` 当独立工具用（已有 52 个测试保底）


---

## 7. 风险与合规

| 风险 | 应对 |
|---|---|
| 站点接口随时失效 | 适配器插件化 + 单源隔离 + 健康度监控（不因一个源挂掉影响全局） |
| 公共 PanSou 实例限流/不稳（实测 400 + timeout） | 生产自建 Docker；公共实例仅作降级备份 |
| 百度验活接口非官方，可能变 | 码表 YAML 化；接口挂掉时降级为"仅标注疑似失效"，不阻断搜索 |
| 抓取频率 / 反爬 | 全局限速、并发上限、随机 UA、失败退避；尊重 `robots.txt` |
| **版权** | 工具**只做公开链接的检索与索引**，不下载、不存储文件、不破解、不绕过验证码；界面加免责声明；结果页优先展示官方渠道（Bandcamp / mora / Steam / 官网）作为合规替代 |
| 转存功能 | 涉及账号与版权，**默认不做**；如确需要，仅保留"手动在新窗口打开" |

---

## 8. 已确认的决策（2026-09-12）

| # | 决策项 | 结论 |
|---|---|---|
| 1 | **形态** | 本地 Web UI + CLI |
| 2 | **技术栈** | Python（复用 `baidu-project/round2/` 的 789 行原型） |
| 3 | **范围** | **百度优先 + 全类型兜底**（夸克/阿里/115/迅雷/123/天翼/UC/磁力） |
| 4 | **PanSou** | 先用公共实例跑通（M1），M4 再自建 Docker |
| 5 | **转存** | **不做**（只检索+验活，规避账号与版权风险） |

→ 从 **M0 + M1** 开始动手。
